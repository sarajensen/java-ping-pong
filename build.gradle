apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "java-file-template"
version = '1.0'
mainClassName = "App"

repositories {
 mavenCentral()
}
/*mavenCentral() is a giant public repository that contains dependencies.*/

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
